# Core audio/video + choreography compile helpers.
numpy>=1.26
scipy>=1.11
# Optional — install for real detect/track:
opencv-python-headless>=4.8
# mediapipe>=0.10
# YouTube fetch (also install ffmpeg on PATH):
# yt-dlp>=2024.8.0
# librosa>=0.10
# soundfile>=0.12
